apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment
spec:
  selector:
    app: {{ .Release.Name }}-payment
  ports:
  - protocol: TCP
    port: 8001
    targetPort: 8001
    nodePort: {{ .Values.payment.service.nodePort }}
  type: {{ .Values.payment.service.type }}
